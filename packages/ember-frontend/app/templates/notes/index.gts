import RouteTemplate from 'ember-route-template';
import Component from '@glimmer/component';
import './index.css';

class NotesIndexComponent extends Component {
  <template>
    <div class=''>
      notes index
    </div>
  </template>
}

export default RouteTemplate(NotesIndexComponent);
